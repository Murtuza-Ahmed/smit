export default function Button({children}) {
    // const { text, children } = props
    // console.log(props)
    return (
        <>
            {/* <button>{text}</button> */}
            <button>{children}</button>
        </>
    )
}